# 5. The Sales Domain

With the Catalog domain handling products, we now move to the **Sales** bounded context to handle Customers and Orders.

*(In a real tutorial, we would walk through building `Customer` and `Order` aggregates here. For now, refer to the `example/e-commerce/internal/sales/` directory in the repository!)*
